# Define a function called count that has two arguments called sequence and item.

#Return the number of times the item occurs in the list.
def count(sequence, item):
    sum = 0
    for x in sequence:
        if x == item:
            sum += 1
    return sum
        
print (count([1, 2, 1, 3], 1))
